# 网关端口
PORT=3000

# JWT密钥（请在生产环境中使用强密钥）
JWT_SECRET=your-secret-key-change-in-production

# 公开前缀列表（不需要认证的路径前缀，用逗号分隔）
# 所有以这些前缀开头的路径都会跳过认证
# 例如：PUBLIC_PREFIXES=/public,/api/public,/open
PUBLIC_PREFIXES=/public,/open,/health
