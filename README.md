# Backend Gateway

基于 NestJS 的 API 网关服务，提供统一的认证、请求转发和错误处理功能。

## 📋 目录

- [功能特性](#功能特性)
- [技术栈](#技术栈)
- [项目结构](#项目结构)
- [核心特性](#核心特性)
- [快速开始](#快速开始)
- [配置说明](#配置说明)
- [API 文档](#api-文档)
- [使用示例](#使用示例)
- [开发指南](#开发指南)

## ✨ 功能特性

### 🔐 认证功能

- **JWT Token 验证**：使用 JWT 密钥验证客户端 Token
- **用户信息提取**：从 Token 中提取 `userId` 和 `username`
- **用户信息注入**：将用户信息通过请求头传递给业务服务
- **灵活的认证控制**：
  - 支持 `@Public()` 装饰器标记公开路由
  - 支持 `PUBLIC_PREFIXES` 环境变量配置公开路径前缀
  - 支持服务级别的 `requiresAuth` 配置

### 🔄 请求代理

- **精确路径匹配**：智能匹配服务路径，避免误匹配（如 `/api/aire` 不会匹配 `/api/aireview`）
- **路径重写**：支持对象和函数两种形式的路径重写规则
- **请求头处理**：自动处理敏感头信息，注入用户信息
- **错误处理**：统一的错误响应格式
- **超时控制**：支持为每个服务配置超时时间

### 🛡️ 错误处理

- **统一响应格式**：所有错误响应统一为 `{ success, code, message, timestamp, path }` 格式
- **HTTP 状态码统一**：所有错误响应 HTTP 状态码为 200，实际状态码通过 `code` 字段表示
- **全局异常捕获**：自动捕获并格式化所有异常

## 🛠️ 技术栈

- **框架**：NestJS 10.x
- **语言**：TypeScript 5.x
- **认证**：@nestjs/jwt
- **代理**：http-proxy-middleware
- **配置管理**：@nestjs/config

## 📁 项目结构

```
src/
├── common/                    # 公共模块
│   ├── decorators/           # 装饰器
│   │   └── public.decorator.ts    # 公开路由装饰器
│   └── filters/              # 过滤器
│       └── http-exception.filter.ts  # 全局异常过滤器
├── config/                    # 配置文件
│   └── services.ts           # 服务配置（路径、目标、认证等）
├── modules/                   # 功能模块
│   ├── auth/                 # 认证模块
│   │   ├── auth.module.ts
│   │   ├── auth.service.ts   # Token 验证服务
│   │   └── guards/
│   │       └── auth.guard.ts # 认证守卫
│   ├── proxy/                # 代理模块
│   │   ├── proxy.module.ts
│   │   └── proxy.service.ts  # 代理服务
│   └── services/             # 路由服务模块
│       ├── services.module.ts
│       └── route.service.ts   # 路径匹配服务
├── app.controller.ts         # 主控制器
├── app.module.ts             # 主模块
└── main.ts                   # 应用入口
```

## 🎯 核心特性

### 1. 精确路径匹配

网关使用精确路径匹配算法，确保路径段完整匹配：

- ✅ `/api/aireview` 匹配 `/api/aireview` 和 `/api/aireview/xxx`
- ❌ `/api/aire` **不匹配** `/api/aireview`（避免误匹配）

### 2. 请求处理流程

```
请求进入
  ↓
检查 @Public() 装饰器 → 是 → 跳过认证
  ↓ 否
检查 PUBLIC_PREFIXES 配置 → 匹配 → 跳过认证
  ↓ 不匹配
匹配服务配置（RouteService）
  ↓
未匹配到服务 → 返回 404
  ↓ 匹配到
检查 requiresAuth
  ↓
requiresAuth !== true → 跳过认证
  ↓ requiresAuth === true
验证 JWT Token
  ↓
提取用户信息 → 注入到请求头
  ↓
代理请求到目标服务
```

### 3. 路径重写

支持两种形式的路径重写：

**对象形式**：

```typescript
pathRewrite: {
  '^/api/aireview': '',  // 移除前缀
  '^/api/v1': '/api/v2',  // 替换前缀
}
```

**函数形式**：

```typescript
pathRewrite: (path, req) => {
  // 自定义逻辑
  return path.replace('/api/aireview', '');
};
```

**注意**：如果不配置 `pathRewrite`，路径会原样传递给目标服务。

## 🚀 快速开始

### 环境要求

- Node.js >= 18.x
- pnpm >= 8.x (或 npm/yarn)

### 安装依赖

```bash
pnpm install
```

### 配置环境变量

创建 `.env` 文件：

```env
# 网关端口
PORT=9999

# JWT密钥（请在生产环境中使用强密钥）
JWT_SECRET=your-secret-key-change-in-production

# 公开路径前缀列表（不需要认证的路径前缀，用逗号分隔）
# 所有以这些前缀开头的路径都会跳过认证
PUBLIC_PREFIXES=/public,/open
```

### 配置服务路由

编辑 `src/config/services.ts`：

```typescript
export const ALL_SERVICES: ServiceConfig[] = [
  {
    path: '/api/aireview', // 路径前缀
    target: 'http://backend-aireview:3000', // 目标服务地址
    requiresAuth: false, // 是否需要认证
    timeout: 10000, // 超时时间（毫秒）
    changeOrigin: true, // 是否修改 Host 头
    pathRewrite: {
      // 路径重写规则（可选）
      '^/api/aireview': '', // 移除 /api/aireview 前缀
    },
  },
  {
    path: '/api/account',
    target: 'http://localhost:3001',
    requiresAuth: true, // 需要认证
    timeout: 10000,
    changeOrigin: true,
    // 未配置 pathRewrite，路径会原样传递
  },
];
```

### 启动服务

```bash
# 开发模式（热重载）
pnpm run start:dev

# 生产模式
pnpm run build
pnpm run start:prod
```

服务将在 `http://localhost:9999` 启动。

## ⚙️ 配置说明

### 环境变量

| 变量名            | 说明                         | 默认值 | 必填 |
| ----------------- | ---------------------------- | ------ | ---- |
| `PORT`            | 网关端口                     | 9999   | 否   |
| `JWT_SECRET`      | JWT 密钥                     | -      | 是   |
| `PUBLIC_PREFIXES` | 公开路径前缀列表（逗号分隔） | -      | 否   |

### 服务配置

每个服务配置包含以下字段：

| 字段           | 类型               | 说明                   | 必填             |
| -------------- | ------------------ | ---------------------- | ---------------- |
| `path`         | string             | 路径前缀，用于匹配请求 | 是               |
| `target`       | string             | 目标服务地址           | 是               |
| `requiresAuth` | boolean            | 是否需要认证           | 否（默认 false） |
| `timeout`      | number             | 超时时间（毫秒）       | 否（默认 30000） |
| `changeOrigin` | boolean            | 是否修改 Host 头       | 否（默认 true）  |
| `pathRewrite`  | object \| function | 路径重写规则           | 否               |

#### pathRewrite 详细说明

**对象形式**：

```typescript
pathRewrite: {
  '^/api/old': '/api/new',  // 替换路径前缀
  '^/remove': '',            // 移除路径前缀
}
```

**函数形式**：

```typescript
pathRewrite: (path: string, req: Request) => string;
```

**注意**：

- 如果不配置 `pathRewrite`，路径会原样传递给目标服务
- 如果需要移除路径前缀，必须显式配置 `pathRewrite`

## 📚 API 文档

### 健康检查

```http
GET /health
```

**响应：**

```json
{
  "status": "ok",
  "timestamp": "2024-11-27T16:35:00.000Z"
}
```

### 代理请求

所有匹配服务配置的请求都会被代理到对应的业务服务。

**请求示例：**

```http
GET /api/account/users/123
Authorization: Bearer <token>
```

**网关处理流程：**

1. 匹配服务配置（`/api/account`）
2. 检查 `requiresAuth`，如果需要则验证 Token
3. 提取用户信息（userId, username）
4. 应用路径重写规则（如果配置了 `pathRewrite`）
5. 转发请求到目标服务
6. 在请求头中添加 `x-user-id` 和 `x-username`
7. 返回业务服务响应

**业务服务接收到的请求头：**

```http
x-user-id: 123
x-username: john
```

## 📝 使用示例

### 1. 公开路由（不需要认证）

#### 方式一：使用装饰器

```typescript
@Get('/public/info')
@Public()
getPublicInfo() {
  return { message: 'This is public' };
}
```

#### 方式二：配置公开前缀

在 `.env` 中配置：

```env
PUBLIC_PREFIXES=/public,/open
```

所有以 `/public` 或 `/open` 开头的路径都会跳过认证。

#### 方式三：服务级别配置

在 `services.ts` 中配置：

```typescript
{
  path: '/api/public',
  target: 'http://localhost:3001',
  requiresAuth: false,  // 不需要认证
}
```

### 2. 需要认证的路由

在服务配置中设置 `requiresAuth: true`，客户端需要在请求头中携带 Token：

```http
GET /api/account/users/123
Authorization: Bearer <your-jwt-token>
```

### 3. 路径重写示例

#### 移除路径前缀

```typescript
{
  path: '/api/aireview',
  target: 'http://backend-aireview:3000',
  pathRewrite: {
    '^/api/aireview': '',  // /api/aireview/webhook/github -> /webhook/github
  },
}
```

#### 替换路径前缀

```typescript
{
  path: '/api/v1',
  target: 'http://localhost:3001',
  pathRewrite: {
    '^/api/v1': '/api/v2',  // /api/v1/users -> /api/v2/users
  },
}
```

#### 自定义路径重写逻辑

```typescript
{
  path: '/api/products',
  target: 'http://localhost:3002',
  pathRewrite: (path, req) => {
    if (path.startsWith('/api/products/v1')) {
      return path.replace('/api/products/v1', '/v1');
    }
    if (path.startsWith('/api/products')) {
      return path.replace('/api/products', '');
    }
    return path;
  },
}
```

### 4. 错误响应格式

所有错误响应统一格式：

```json
{
  "success": false,
  "code": 401,
  "message": "Token not found",
  "timestamp": "2024-11-27T16:35:00.000Z",
  "path": "/api/account/users"
}
```

**注意**：HTTP 状态码统一为 200，实际状态码通过 `code` 字段表示。

### 5. 常见错误码

| code | 说明                         |
| ---- | ---------------------------- |
| 401  | 认证失败（Token 无效或缺失） |
| 404  | 服务未找到或路由不存在       |
| 502  | 目标服务不可用               |
| 500  | 服务器内部错误               |

## 🔧 开发指南

### 添加新服务

1. 在 `src/config/services.ts` 中添加服务配置：

```typescript
{
  path: '/api/new-service',
  target: 'http://localhost:3004',
  requiresAuth: true,
  timeout: 10000,
  changeOrigin: true,
  pathRewrite: {
    '^/api/new-service': '',  // 可选：移除路径前缀
  },
}
```

2. 重启服务即可生效。

### 模块说明

#### ServicesModule

提供路径匹配服务（`RouteService`），负责：

- 加载服务配置
- 精确路径匹配
- 查找目标服务

#### AuthModule

提供认证功能：

- JWT Token 验证
- 用户信息提取
- 认证守卫（`AuthGuard`）

#### ProxyModule

提供代理功能：

- 创建代理中间件
- 处理请求头
- 路径重写
- 错误处理

### 自定义错误处理

修改 `src/common/filters/http-exception.filter.ts` 来自定义错误响应格式。

### 添加中间件

在 `src/main.ts` 中添加全局中间件：

```typescript
app.use(cors());
app.use(helmet());
```

### 运行测试

```bash
# 单元测试
pnpm run test

# 测试覆盖率
pnpm run test:cov

# E2E 测试
pnpm run test:e2e
```

## 📦 构建和部署

### 构建

```bash
pnpm run build
```

构建产物在 `dist/` 目录。

### 生产环境部署

1. 设置环境变量（特别是 `JWT_SECRET`）
2. 构建项目
3. 运行：

```bash
pnpm run start:prod
```

### Docker 部署

项目已包含 `Dockerfile` 和 `docker-compose.yml`，可以直接使用：

```bash
# 使用 Docker Compose
docker-compose up -d

# 或使用 Docker
docker build -t backend-gateway .
docker run -p 9999:9999 backend-gateway
```

## 🔒 安全建议

1. **JWT 密钥**：生产环境必须使用强密钥，建议至少 32 字符
2. **HTTPS**：生产环境必须使用 HTTPS
3. **CORS**：根据实际需求配置 CORS
4. **限流**：建议添加限流中间件防止 API 滥用
5. **日志**：生产环境建议添加日志收集
6. **路径匹配**：使用精确路径匹配避免误匹配

## 📄 许可证

UNLICENSED

## 🤝 贡献

欢迎提交 Issue 和 Pull Request。

## 📞 联系方式

如有问题，请提交 Issue。
